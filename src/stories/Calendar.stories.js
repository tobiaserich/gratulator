import React from "react";
import CalendarComponent from "../components/Calendar";
export default { title: "components", component: CalendarComponent };

export const Calendar = () => {
  return <CalendarComponent />;
};
